{
  "name": "dotclaude",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Claude Code configuration — skills, agents, and instructions",
  "type": "module",
  "author": "Kassaila",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{md,json,sh}": "prettier --write"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "prettier-plugin-sh": "^0.18.1"
  }
}
